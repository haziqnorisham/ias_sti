info:
  name: Get Tree Sensor Angle
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseUrl}}/GET_TREE_SENSOR_ANGLE"
  params:
    - name: dev_eui
      value: ""
      type: query
      disabled: false

settings:
  encodeUrl: true
  followRedirects: true
